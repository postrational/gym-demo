language: python

git:
  depth: false

os: linux
sudo: false

script: 
  - tox

install:
  - pip install tox

branches:
  only:
  - "master"

cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/pre-commit
