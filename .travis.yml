language: python

git:
  depth: false

matrix:
  include:
  - os: linux
    python:
    - "3.7"
  - os: osx
    python:
    - "3.7"

install:
  - pip install tox

script:
  - tox

branches:
  only:
  - "master"
